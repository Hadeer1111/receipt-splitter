{
  "name": "receipt-splitter",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @receipt-splitter/web dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=20"
  }
}
